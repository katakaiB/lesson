// Переменная X
int X

// Константа L
const int L = 0; // Здесь может быть любое другое значение

// Условие для проверки a < b
if (a < b) {
X = a / b + L;
} else if (a == b) {
X = -71;
} else {
X = (a - b) / a;
}
